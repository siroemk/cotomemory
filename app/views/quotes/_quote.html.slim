= turbo_frame_tag quote do
  .py-4.px-2.mb-2.bg-white.rounded.relative
    .flex.pb-2
      p.text-sm.text-slate-600.font-semibold
        = quote.child.name
      p.text-sm.text-slate-600.font-semibold
        = "（#{year_of_birth(quote.child.date_of_birth)}才#{month_of_birth(quote.child.date_of_birth)}ヶ月）"
      p.text-sm.text-slate-400
        = l quote.created_at, format: :long
      - if current_user == quote.user
        .ml-auto.flex.mr-1
          p.text-sm.text-slate-400
            = link_to '編集', edit_quote_path(quote), class: 'text-xs cursor-pointer mr-2'
          p.text-sm.text-slate-400
            = link_to '削除', quote, data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' }, class: 'text-xs cursor-pointer'

    p.leading-relaxed.text-slate-800.text-base
      = quote.content

    .flex.justify-end
      = link_to quote_path(quote.id), data: { turbo: false }, class: 'cursor-pointer pr-1'
        .flex
          .pr-1
            = image_tag 'comment-icon.png', alt: 'コメントの画像', class: 'h-4'
          p.text-slate-500.text-xs
            = quote.comments.count
